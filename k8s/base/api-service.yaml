apiVersion: v1
kind: Service
metadata:
  name: opt-sim-api
  labels:
    app: opt-sim-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: opt-sim-api
